# Dovecot LDAP Configuration
# Generated by Ansible
# Compatible with Dovecot 2.4.1+
# Included via !include in /etc/dovecot/conf.d/auth-ldap.conf.ext
#
# LDAP Server: int-srv (192.168.1.10)
#
# ALL settings in Dovecot 2.4 use ldap_ prefix where they didn't before:
#   hosts   -> ldap_uris
#   dn      -> ldap_auth_dn
#   dnpass  -> ldap_auth_dn_password
#   base    -> ldap_base
#   scope   -> ldap_scope

ldap_uris             = ldap://192.168.1.10
ldap_auth_dn          = cn=admin,dc=lksn2025,dc=id
ldap_auth_dn_password = Skills39!
ldap_version          = 3
ldap_base             = ou=People,dc=lksn2025,dc=id
ldap_scope            = subtree
auth_bind        = yes
auth_bind_userdn = uid=%{user},ou=People,dc=lksn2025,dc=id
ldap_user_filter      = (&(objectClass=posixAccount)(uid=%{user}))
ldap_user_attrs       = homeDirectory=home,uidNumber=uid,gidNumber=gid
